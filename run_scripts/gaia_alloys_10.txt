effect { while = { count = 80 limit = { free_district_slots > 0 } add_district = district_city } add_zone = { district = district_city zone = zone_foundry zone_slot = 2 replace = yes } add_zone = { district = district_city zone = zone_foundry zone_slot = 3 replace = yes } }
