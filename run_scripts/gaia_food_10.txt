effect { while = { count = 80 limit = { free_district_slots > 0 num_free_districts = { type = district_farming value > 0 } } add_district = district_farming } add_zone = { district = district_farming zone = zone_food zone_slot = 1 replace = yes } }
